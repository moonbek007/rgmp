import React from "react";

const ReactCreateElement = React.createElement(
  "div",
  null,
  "This component has been created using React.createElement"
);

export default ReactCreateElement;
